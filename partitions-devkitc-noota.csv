# DustGate — ESP32-DevKitC (WROOM-32, 4 MB) partition table
#
# No OTA (the device is flashed over USB, not over-the-air) and no TinyUF2
# bootloader (that's Adafruit-only) — so the two wasteful 1408 KB OTA app slots
# and the 256 KB uf2 partition of the stock/Feather layouts are reclaimed for a
# single large app slot plus a large filesystem.
#
#   app0  1.75 MB  — currently ~1.20 MB used → ~0.55 MB headroom for Phase-2 firmware
#   ffat  2.19 MB  — LittleFS for the Angular bundle; ~0.44 MB used today → ~5x
#                    headroom for the much larger Phase-2 UI
#
# The filesystem partition MUST be labeled "ffat": HttpApiServer mounts it with
# LittleFS.begin(true, "/littlefs", 10, "ffat"). (LittleFS mounts by label
# regardless of the "fat" subtype — same as the Feather's stock table.)
#
# Layout fills a 4 MB flash exactly (0x400000). If you ever source an 8/16 MB
# DevKitC, OTA becomes feasible again — that would be a different table.
#
# Name,   Type, SubType, Offset,   Size,      Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x1C0000,
ffat,     data, fat,     0x1D0000, 0x230000,
