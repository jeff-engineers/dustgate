{
  "buildCommand": "cd dustgate-ui && npm ci && npm run build",
  "outputDirectory": "dustgate-ui/dist/dustgate-ui/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
