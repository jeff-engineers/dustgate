{
  "buildCommand": "cd dustgate-ui && npm install && npm run build",
  "outputDirectory": "dustgate-ui/dist/dustgate-ui/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
