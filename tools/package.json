{
  "name": "dustgate-tools",
  "version": "1.0.0",
  "description": "Dev tools for DustGate — mock API server and provisioning utilities",
  "scripts": {
    "mock": "node mock-api.js",
    "mock:claude": "ANTHROPIC_KEY=$(cat ~/.anthropic-key 2>/dev/null || echo '') node mock-api.js"
  },
  "dependencies": {
    "ws": "^8.14.2"
  }
}
